'use client';

import { useQuery } from '@tanstack/react-query';
import { useState } from 'react';
import { api, ApiError } from '../lib/api';
import { formatDate, formatLiters } from '../lib/dashboard-utils';
import { AdminShell } from './admin-shell';
import { Badge, EmptyState, ErrorState, Skeleton } from './ui';

export function MerchantsView() {
  const [search, setSearch] = useState('');
  const [anomaly, setAnomaly] = useState(false);
  const [selectedId, setSelectedId] = useState<string | null>(null);
  const result = useQuery({ queryKey: ['merchants', search, anomaly], queryFn: () => api.merchants({ search: search || undefined, anomaly: anomaly || undefined }) });
  const containers = useQuery({ queryKey: ['merchant-containers', selectedId], queryFn: () => api.containers({ merchant_id: selectedId ?? undefined }), enabled: Boolean(selectedId) });
  const selectedMerchant = result.data?.data.find((merchant) => merchant.id === selectedId);
  if (result.isLoading) return <AdminShell><Skeleton className="h-10 w-48" /><Skeleton className="mt-6 h-96" /></AdminShell>;
  if (result.error) return <AdminShell><ErrorState message={result.error instanceof ApiError ? result.error.message : 'Không thể tải danh sách quán.'} /></AdminShell>;
  return <AdminShell><p className="text-sm font-semibold text-emerald-700">Mạng lưới điểm thu gom</p><h2 className="mt-1 text-3xl font-bold">Quán</h2><div className="mt-6 flex flex-wrap gap-3"><input className="min-h-11 min-w-64 rounded-xl border border-slate-300 bg-white px-3" placeholder="Tìm tên quán…" value={search} onChange={(event) => setSearch(event.target.value)} /><label className="flex min-h-11 items-center gap-2 rounded-xl border border-slate-300 bg-white px-3 text-sm"><input type="checkbox" checked={anomaly} onChange={(event) => setAnomaly(event.target.checked)} /> Chỉ hiện bất thường</label></div><section className="mt-5 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm">{!result.data?.data.length ? <EmptyState message="Không tìm thấy quán phù hợp." /> : <div className="overflow-x-auto"><table className="w-full min-w-[980px] text-left text-sm"><thead className="border-b text-xs uppercase text-slate-400"><tr><th className="pb-3">Tên quán</th><th className="pb-3">Địa chỉ</th><th className="pb-3">Tọa độ</th><th className="pb-3">Cách trạm</th><th className="pb-3">Trung bình/ngày</th><th className="pb-3">Lần thu gần nhất</th><th className="pb-3">Can</th><th className="pb-3">Ghi chú</th></tr></thead><tbody>{result.data.data.map((merchant) => <tr key={merchant.id} className="border-b last:border-0"><td className="py-4 font-semibold">{merchant.name}</td><td className="py-4 text-slate-600">{merchant.address ?? '—'}</td><td className="py-4 text-slate-500">{merchant.lat?.toFixed(4) ?? '—'}, {merchant.lng?.toFixed(4) ?? '—'}</td><td className="py-4">{merchant.distance_m === null ? '—' : `${(merchant.distance_m / 1000).toFixed(1)} km`}</td><td className="py-4">{merchant.avg_daily_liters === null ? '—' : formatLiters(merchant.avg_daily_liters)}</td><td className="py-4">{formatDate(merchant.last_collected_at)}</td><td className="py-4"><button className="min-h-10 rounded-lg border border-emerald-700 px-3 font-semibold text-emerald-700" onClick={() => setSelectedId(selectedId === merchant.id ? null : merchant.id)}>{selectedId === merchant.id ? 'Đóng' : 'Xem can'}</button></td><td className="py-4">{merchant.anomaly ? <Badge tone="red">Bất thường</Badge> : <Badge tone="green">Bình thường</Badge>}</td></tr>)}</tbody></table></div>}</section>{selectedMerchant && <section className="mt-5 rounded-2xl border border-emerald-200 bg-emerald-50 p-5"><div className="flex flex-wrap items-center justify-between gap-2"><div><p className="text-sm text-emerald-700">Chi tiết quán</p><h3 className="text-xl font-bold">{selectedMerchant.name}</h3></div><span className="text-sm text-slate-600">{selectedMerchant.address ?? 'Chưa có địa chỉ'}</span></div>{containers.isLoading ? <Skeleton className="mt-4 h-20" /> : containers.error ? <p className="mt-4 text-sm text-red-700">Không thể tải danh sách can.</p> : !containers.data?.data.length ? <p className="mt-4 rounded-xl bg-white p-4 text-sm text-slate-600">Quán chưa được cấp can.</p> : <div className="mt-4 grid gap-3 md:grid-cols-2">{containers.data.data.map((container) => <div key={container.id} className="rounded-xl bg-white p-4"><div className="flex items-center justify-between gap-2"><strong className="font-mono text-sm">{container.qr_code}</strong><Badge tone={container.state === 'AT_MERCHANT' ? 'green' : 'orange'}>{container.state}</Badge></div><p className="mt-2 text-sm text-slate-600">Dung tích: {container.capacity_liters ?? '—'} lít</p></div>)}</div>}</section>}</AdminShell>;
}
